    # Loại trừ thư mục node_modules
    node_modules
    
    # Loại trừ thư mục logs
    logs
    
    # Loại trừ file .env
    .env
    
    # Loại trừ thư mục .git
    .git
    
    # Loại trừ Dockerfile và .dockerignore
    Dockerfile
    .dockerignore
    
    # Giữ lại thư mục public (nếu có)
    !public/
    
    # Loại trừ tất cả các file .log
    *.log